# Node.js dependencies
node_modules/

# Yarn specific files
.yarn/
.yarnrc.yml
.pnp.*

# Next.js build and static export
.next/
out/

# TypeScript build info files
*.tsbuildinfo

# NestJS build output
dist/
build/

# Logs (e.g. npm, yarn, general logs)
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE configuration files (e.g. VSCode, JetBrains)
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Jest coverage and test results
coverage/

# Environment configuration files (containing sensitive data like API keys, passwords)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Temporary files created by editors or OS (e.g. swap files, MacOS, Windows)
*.swp
*.swo
*.DS_Store
Thumbs.db

# Test results (if using CI/CD pipelines)
test-results/

# Docker-related files (if applicable)
docker-compose.override.yml

# Lock files (npm/yarn specific, sometimes ignored based on team preference)
package-lock.json
yarn.lock

# TypeScript and Babel configuration files
tsconfig.json
tsconfig.build.json
babel.config.js

# TurboRepo specific
.turbo/